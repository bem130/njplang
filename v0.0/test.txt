数値型の定数「数字5」を5と定める
数値型変数「ご」を定義して「5」を代入する
ニ格の数値「数1」とヲ格の数値「数2」を引数に取る、数値を返すサ変関数「足す」を以下のように定義する
    「3とAを足してから4と3を足したもの」をBに代入する
    返り値Bとして関数を終了する
「3とAを足してから4と3を足したもの」を5に足す
"もじれつ"に"B"を足す
単位「回」の数値「回数」とヲ格の数値「数」を引数に取る、数値を返すサ変関数「足す」を以下のように定義する
「n」を0始まりのカウンタとして、以下をN回繰り返す
もしもAが真ならば、以下を実行する
    10回5を足す